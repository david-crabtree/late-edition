---
name: An agent's command line changed
about: A provider stopped working because its CLI moved a flag or its output format
labels: bug, provider
---

Late Edition drives each agent through its command line. When a CLI changes, the desk
using it stops, and that is a release blocker here.

**Agent and version** (`claude --version`, `codex --version`, and so on)

**What Late Edition said** when the desk failed

**What the CLI now expects**, if you know (a link to its changelog or docs is ideal)
